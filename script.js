const GameBoard = (() => {
    /*
    - Display gameboard in DOM
    - Update board UI
     */
    const board = []

})()

const GameController = (() => {
    /*
    - Make Player move
    - Reset game
    - Determine winner or tie
     */
})()

const Player = (sign) => {
    /*
    - Get sign
     */
}
